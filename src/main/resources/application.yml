spring:
  application:
    name: praesto
  ai:
    openai:
      api-key: ${OPEN_AI_KEY}

  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: Praesto

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://${AUTH0_DOMAIN}/

