// Registrierung läuft ausschliesslich über Einladungslinks (/join/{token}).
// Diese Seite ist daher rein informativ – kein eigener Signup-Flow mehr.
export const load = async () => {
	return {};
};
